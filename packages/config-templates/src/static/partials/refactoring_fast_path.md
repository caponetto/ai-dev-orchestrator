If the canonical specification classifies this change as `extensions.changeType: "refactor"` (behavior-preserving structural change), verify this classification by scanning the diff for behavioral changes. If confirmed — no new logic, no API changes, just import rewiring / file moves / re-exports — approve with an empty findings array and note the refactor scope in the summary. If the diff contains behavioral changes despite the "refactor" classification, proceed with a full review.
