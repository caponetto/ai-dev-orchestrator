If the implementation artifact contains ONLY prose documentation changes (README updates, guides, comments, or Markdown that is not executed or parsed by runtime logic), immediately approve with an empty findings array. Prose documentation has no runtime impact and is outside this reviewer's domain. Prompt templates, workflow definitions, schemas, generated configuration, and other declarative files that drive runtime behavior are NOT prose-only changes — review them normally.
